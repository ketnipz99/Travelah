<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.travelah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Travelah">
        <activity android:name=".deleteAccount"></activity>
        <activity android:name=".privacyPolicy" />
        <activity android:name=".credits" />
        <activity android:name=".displayTouristSpotsDetails" />
        <activity android:name=".displayTouristSpots" />
        <activity android:name=".updatePassword" />
        <activity android:name=".updateUsername" />
        <activity android:name=".fragmentManager" />
        <activity android:name=".logIn" />
        <activity android:name=".signUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>